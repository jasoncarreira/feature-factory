# dependencies
node_modules/

# opencode run state + worktrees (per-repo control plane, never committed here)
.opencode/

# build / coverage / caches
dist/
coverage/
.cache/

# local env / secrets (never commit)
.env
.env.*
*.pem
*.key

# os / editor
.DS_Store
*.log
